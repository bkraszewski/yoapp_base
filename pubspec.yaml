name: yoapp
description: Flutter version of YO! app
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.12.7+1
  firebase_messaging: 5.1.2
  cupertino_icons: ^0.1.2

  http: ^0.12.0+2
  audioplayers: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/yo_sound.mp3 
